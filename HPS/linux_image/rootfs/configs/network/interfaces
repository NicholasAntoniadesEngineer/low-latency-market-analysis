# Network configuration for DE10-Nano
# This file can be customized or will be auto-generated by build script

auto lo
iface lo inet loopback

# Ethernet interface - DHCP by default
# Change to static IP by modifying build_config.sh or build_rootfs.sh
auto eth0
iface eth0 inet dhcp

# Example static IP configuration (uncomment and modify as needed):
# auto eth0
# iface eth0 inet static
#     address 192.168.1.100
#     netmask 255.255.255.0
#     gateway 192.168.1.1
